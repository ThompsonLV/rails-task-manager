<h2>Listing de mes tâches</h2>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task)%>
      <%= check_box_tag "task_completed_#{task.id}", 1, task.completed %>
    </li>
  <% end %>
</ul>

<p><%= link_to "New task", new_task_path %></p>
